export const ValidateAddress = (
  address: string,
  structureApi: string,
  reverseApi: string,
) => {
  const opts = {
    format: 'jsonv2',
    addressdetails: 1,
    'accept-language': 'vi',
  };
};
